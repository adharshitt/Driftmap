[package]
name = "driftmap-tui"
version = "0.1.0"
edition = "2021"

[dependencies]
driftmap-core = { path = "../driftmap-core" }
ratatui = "0.26"
crossterm = "0.27"
tokio = { version = "1", features = ["full"] }
anyhow = "1"
